import { PendingTasksPipe } from './pending-tasks.pipe';

describe('PendingTasksPipe', () => {
  it('create an instance', () => {
    const pipe = new PendingTasksPipe();
    expect(pipe).toBeTruthy();
  });
});
